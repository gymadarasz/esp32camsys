import { WifiCredentialList } from './wifi-credential-list';

describe('WifiCredentialList', () => {
  it('should create an instance', () => {
    expect(new WifiCredentialList()).toBeTruthy();
  });
});
